version: '3.8'

services:
  web:
    build: .
    ports:
      - "80:3000"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
    env_file:
      - .env
    volumes:
      # Mount a local tmp folder so PDFs don't persist in the container layer
      - ./tmp:/app/tmp
